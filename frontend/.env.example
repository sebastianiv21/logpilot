# Backend API base URL (no trailing slash). Used by the frontend API client.
VITE_API_BASE=http://localhost:8000

# Grafana (or metrics dashboard) URL. Opened in a new tab with session context.
VITE_GRAFANA_URL=http://localhost:3000
