# Backend API base URL (no trailing slash). Used by the frontend API client.
VITE_API_BASE=http://localhost:8000

# Grafana base URL (e.g. http://localhost:3000). Opens grafana-lokiexplore-app logs view.
VITE_GRAFANA_URL=http://localhost:3000
