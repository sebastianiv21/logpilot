---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# logpilot Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-15

## Active Technologies
- TypeScript 5.x (frontend), React 18.x; backend remains Python 3.11+ + Vite, React, React Router, Tailwind CSS, DaisyUI, TanStack Query, Zod, React Hook Form, date-fns, Sonner; backend: FastAPI (existing) (002-frontend-implementation)
- None on frontend; session/report state from backend API; optional local state (e.g. current session id in memory or localStorage for UX) (002-frontend-implementation)
- TypeScript 5.x (frontend), Python 3.11+ (backend); unchanged from 002 + Frontend: React, ReactMarkdown (or equivalent), Tailwind CSS, DaisyUI; Backend: FastAPI, markdown, ReportLab (existing for PDF) (003-report-visualization-improvement)
- N/A (report content from existing API; no new persistence) (003-report-visualization-improvement)
- TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner (toasts); lucide-react (icons, to be added if not present) (004-ui-polish-feedback)
- N/A (no new persistence) (004-ui-polish-feedback)
- TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner; lucide-react (Database icon) (005-kb-config-route-layout)
- N/A (no new persistence; KB status from existing API) (005-kb-config-route-layout)
- TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, theme-change, TanStack Query, Sonner, lucide-reac (006-layout-navbar-theme)
- Browser localStorage for theme preference (via theme-change; key `theme` or configurable). No server-side storage. (006-layout-navbar-theme)
- TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner, lucide-react; add a charts library (see research.md) (007-layout-single-screen-cleanup)
- No new server storage; optional localStorage for batch-size preference (implementation choice) (007-layout-single-screen-cleanup)
- TypeScript 5.x (frontend); Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner (toasts), lucide-reac (008-scrollable-session-list-titles)
- No new persistence; current session from SessionContext + sessions from existing API (008-scrollable-session-list-titles)
- TypeScript 5.x (frontend); Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner, lucide-reac (009-session-search-bar)
- No new persistence; filter query is ephemeral UI state (search input value) (009-session-search-bar)
- TypeScript 5.x (frontend), Python 3.11+ (backend) + React 18.x, TanStack Query, FastAPI; existing SessionContext, UploadLogs, upload API (010-upload-session-state-fix)
- Backend: SQLite (existing sessions + session_log_extent); add storage for last upload result per session (new table or columns). Frontend: no new persistence; continue storing currentSessionId in localStorage. (010-upload-session-state-fix)
- TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner (toast), lucide-reac (011-report-ready-alert-session)
- N/A (session/report from existing API; optional localStorage for current session already exists) (011-report-ready-alert-session)

- Python 3.11+ (typed) + FastAPI, Loki client (e.g. promtail or push API), Prometheus client, Grafana provisioning API, Qdrant client, OpenAI-compatible LLM client (configurable base URL + API key) (001-log-investigation-mvp)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11+ (typed): Follow standard conventions

## Recent Changes
- 011-report-ready-alert-session: Added TypeScript 5.x (frontend), Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner (toast), lucide-reac
- 010-upload-session-state-fix: Added TypeScript 5.x (frontend), Python 3.11+ (backend) + React 18.x, TanStack Query, FastAPI; existing SessionContext, UploadLogs, upload API
- 009-session-search-bar: Added TypeScript 5.x (frontend); Python 3.11+ (backend unchanged) + React 18.x, React Router, Vite, Tailwind CSS, DaisyUI, TanStack Query, Sonner, lucide-reac


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
